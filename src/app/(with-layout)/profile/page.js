import React from 'react';
export const metadata = {
    title: 'Profile || Learn-next',
    description: 'next app',
  }
const ProfilePage = () => {
    return (
        <div>
            ProfilePage
        </div>
    );
};

export default ProfilePage;